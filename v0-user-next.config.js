/** @type {import('next').NextConfig} */
const userConfig = {
  // Add any custom Next.js configuration here
  // Note: swcMinify is no longer needed in Next.js 15.1.0 as it's the default
}

export default userConfig 